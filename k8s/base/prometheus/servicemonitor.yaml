apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ml-training-api
  namespace: ml-training
  labels:
    app: ml-training-api
spec:
  selector:
    matchLabels:
      app: ml-training-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
