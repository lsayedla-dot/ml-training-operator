apiVersion: v1
kind: Service
metadata:
  name: ml-training-api
  namespace: ml-training
  labels:
    app: ml-training-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ml-training-api
